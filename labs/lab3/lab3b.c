#include <stdio.h>
#include <math.h>
int main(void)
{
    // initializing variables //

    int noSides, radius;
    float angleRad, angleDeg, sideLen, periIn, periFt, partialResults;

    printf("Number of sides: ");
    scanf("%i", &noSides);

    printf("Radius: ");
    scanf("%i", &radius);
}